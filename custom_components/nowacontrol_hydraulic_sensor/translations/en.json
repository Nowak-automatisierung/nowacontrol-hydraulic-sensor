{
  "config": {
    "step": {
      "user": {
        "title": "nowaControl Hydraulic Sensor",
        "description": "Set up the nowaControl helper integration for ZHA quirk configuration status and UI-first setup.",
        "data": {
          "path": "Custom ZHA quirk path",
          "auto_install_quirk": "Auto-install disabled (security commissioning required)",
          "show_notifications": "Show setup and restart notifications"
        }
      }
    },
    "abort": {
      "single_instance_allowed": "The nowaControl Hydraulic Sensor integration is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "nowaControl Hydraulic Sensor",
        "description": "Adjust the quirk path and UI behavior.",
        "data": {
          "path": "Custom ZHA quirk path",
          "auto_install_quirk": "Auto-install disabled (security commissioning required)",
          "show_notifications": "Show setup and restart notifications"
        }
      }
    }
  },
  "issues": {
    "zha_custom_quirks_path": {
      "title": "ZHA custom_quirks_path missing or mismatched",
      "description": "The ZHA configuration is missing or does not match the effective nowaControl configuration. Check the ZHA settings. The integration does not write quirk files; deployment requires separate security and commissioning approval."
    },
    "quirk_not_installed": {
      "title": "nowaControl ZHA quirk is not installed yet",
      "description": "Integration product file writes are disabled. Any future deployment requires a separate security and commissioning contract."
    },
    "restart_required": {
      "title": "Obsolete Home Assistant restart notice",
      "description": "This legacy notice is removed while auto-install is disabled. The integration does not install or modify quirk files."
    }
  }
}
